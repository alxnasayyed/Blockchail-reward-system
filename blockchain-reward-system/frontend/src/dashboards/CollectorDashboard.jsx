import React from "react";
import { Container, Typography } from "@mui/material";

const CollectorDashboard = () => {
  return (
    <Container>
      <Typography variant="h3">Collector Dashboard</Typography>
      <Typography>View pickup requests, manage schedules, and track collected waste.</Typography>
    </Container>
  );
};

export default CollectorDashboard;
